import { PreventInvalidInputDirective } from './prevent-invalid-input.directive';

describe('PreventInvalidInputDirective', () => {
  it('should create an instance', () => {
    const directive = new PreventInvalidInputDirective();
    expect(directive).toBeTruthy();
  });
});
